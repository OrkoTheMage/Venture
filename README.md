# Venture — Minimal Python CLI Adventure

Quick start:

Install editable locally:

```bash
python -m pip install -e .
```

Run the game:

```bash
venture
```

Commands: `look`, `go <dir>`, `take <item>`, `inv`, `help`, `quit`.
